<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">
    <solid android:color="@android:color/transparent"/> <!-- Background color -->
    <stroke android:width="0dp" android:color="@android:color/transparent"/> <!-- Remove other borders -->
    <padding android:bottom="5dp"/> <!-- Optional: adjust bottom padding if needed -->
    <corners android:radius="0dp"/> <!-- Ensure corners are not rounded -->
</shape>
